#!/usr/bin/env bash

# apt
alias apt-i='sudo apt-get install -y --show-progress'
alias apt-d='sudo apt-get remove -y --show-progress'
alias apt-u='sudo apt-get update -y && sudo apt-get upgrade -y'
alias apt-r='sudo add-apt-repository -y -u'